<Page x:Class="IDEn.App.Views.UsuariosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Usuarios">
    <Grid Background="#F4F6F8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#EEF2F7" Grid.Column="0">
            <StackPanel Margin="16">
                <TextBlock Text="IDEn" FontSize="20" FontWeight="Bold" Margin="4,8,0,16"/>
                <Button Content="← Configuración" Click="Back" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Column="1" Margin="28,24,28,24">
            <TextBlock Text="Gestión de Usuarios" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="(Pendiente: autenticación/roles. De momento, placeholder.)" Foreground="#6B7280" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</Page>

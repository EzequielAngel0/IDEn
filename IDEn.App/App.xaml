<Application x:Class="IDEn.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Colores -->
        <SolidColorBrush x:Key="BgMain" Color="#F7FAFC"/>
        <SolidColorBrush x:Key="SidebarBg" Color="#F7FAFC"/>
        <SolidColorBrush x:Key="NavActive" Color="#E8EDF2"/>
        <SolidColorBrush x:Key="TextMain" Color="#0D141C"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#4D7399"/>

        <!-- Tipografía (requiere Inter instalada; si no, usa Segoe UI) -->
        <FontFamily x:Key="UiFont">Inter, Segoe UI</FontFamily>

        <!-- Estilos de texto rápidos -->
        <Style x:Key="H1" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
        </Style>
        <Style x:Key="Caption" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Application.Resources>
</Application>
